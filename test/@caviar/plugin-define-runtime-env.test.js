const test = require('ava')
const log = require('util').debuglog('@caviar/plugin-define-runtime-env')
const @caviar/plugin_define_runtime_env = require('../src')

test('description', t => {
  t.is(true, true)
})
